import * as React from "react";

import './CreateSprint.scss';

export class CreateSprint extends React.Component<CreateSprint.Props, CreateSprint.State> {
    public render(): JSX.Element {
        return (
            <h2>Create Sprint</h2>
        )
    }
}